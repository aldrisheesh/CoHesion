
  # Adaptive Workspace App

  This is a code bundle for Adaptive Workspace App. The original project is available at https://www.figma.com/design/v4DV5EVpIowQhZnjuGx9ZC/Adaptive-Workspace-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  